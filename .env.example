# Frontend env (Create React App).
# Leave empty to use localStorage (no backend). Set to your API URL to use the backend.
# Local dev:    REACT_APP_API_URL=http://localhost:3000
# Production:   REACT_APP_API_URL=https://api.your-domain.com
REACT_APP_API_URL=
